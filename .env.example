# === Database ===
DATABASE_URL=postgres://ghr_admin:ghr_secure_pass_2024@localhost:5432/ghr_financial

# === Ollama (Local) ===
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=deepseek-r1:8b

# === WebAuthn ===
# RP_ID should match your domain (use ngrok domain for mobile testing)
RP_ID=localhost
RP_ORIGIN=http://localhost:3000

# === Encryption ===
# Generate with: openssl rand -hex 32
AES_ENCRYPTION_KEY=replace_with_64_hex_chars

# === Server ===
BACKEND_PORT=8080
FRONTEND_URL=http://localhost:3000
